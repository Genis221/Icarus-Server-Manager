{
  "name": "icarus-server-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Web control panel for Icarus dedicated servers.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs --no-open",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
